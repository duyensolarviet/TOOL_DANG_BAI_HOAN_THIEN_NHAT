# Project Rules

- KHÔNG BAO GIỜ được tự ý upload/push mã nguồn lên GitHub (hoặc thực hiện lệnh `git push`) khi chưa có lệnh hoặc sự đồng ý rõ ràng từ người dùng.
- LUÔN LUÔN cung cấp khối code `python src/main.py` ở cuối mỗi câu trả lời để người dùng dễ dàng copy (không được tự động chạy lệnh này).

